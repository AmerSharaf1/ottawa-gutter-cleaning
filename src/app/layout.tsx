import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ottawa Gutter Cleaning & Exterior Services | Professional Gutter Services",
  description: "Professional gutter cleaning, repair, and pressure washing services in Ottawa. Keep your home protected with our expert exterior services. Call 819-328-1394 for a free quote.",
  keywords: "gutter cleaning Ottawa, gutter repair, pressure washing, exterior services, Ottawa",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
